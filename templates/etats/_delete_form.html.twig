<form style="display: none;" id="logout-form" method="post" action="{{ path('etats_delete', {'id': etat.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ etat.id) }}">
</form>
