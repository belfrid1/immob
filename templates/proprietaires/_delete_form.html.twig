<form style="display: none;" id="logout-form" method="post" action="{{ path('proprietaires_delete', {'id': proprietaire.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ proprietaire.id) }}">
</form>
