<form style="display: none;" id="logout-form" method="post" action="{{ path('type_biens_delete', {'id': type_bien.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ type_bien.id) }}">
</form>
